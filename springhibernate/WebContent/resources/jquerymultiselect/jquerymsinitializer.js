function initJQMultiSelect(elementIdToReplace) {
	$(document).ready(function() {
		$("#" + elementIdToReplace).multiSelect();
		// Define all required functions here
		// $('#myselect').multiSelect('select_all');
	});
}